% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_poly_attr_for_point.R
\name{get_poly_attr_for_point}
\alias{get_poly_attr_for_point}
\title{Extract Polygon Attributes for Point Locations}
\usage{
get_poly_attr_for_point(
  point,
  poly,
  iteraction = NULL,
  geom = c("lon", "lat"),
  crs = "epsg:4326",
  outside = TRUE
)
}
\arguments{
\item{point}{A data frame or \code{SpatVector} of point features. If a data frame, it must contain
columns for longitude and latitude specified in \code{geom}.}

\item{poly}{A \code{SpatVector} object representing polygons with attributes to extract.}

\item{iteraction}{Optional. A label (e.g., an ID or name) printed during execution to indicate
which subset is being processed. Useful when running the function in a loop or batch.}

\item{geom}{A character vector of length two specifying the names of the longitude and
latitude columns (default is \code{c("lon", "lat")}).}

\item{crs}{A character string specifying the CRS of the input coordinates (default is \code{"epsg:4326"}).}
}
\value{
A tibble containing the original point data and the corresponding polygon attributes.
For points outside all polygons, attributes are taken from the nearest polygon.
}
\description{
This function intersects point coordinates with a polygon layer and returns polygon attributes
for each point. If a point falls outside all polygons, the function assigns attributes from
the nearest polygon.
}
\details{
The function performs an exact spatial intersection. If no intersection is found,
it finds the nearest polygon using \code{sf::st_nearest_feature()} and merges its attributes.
}
