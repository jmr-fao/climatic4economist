% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill_missing_rast.R
\name{fill_missing_rast}
\alias{fill_missing_rast}
\title{Fill Missing Raster Values Using Neighboring Cells}
\usage{
fill_missing_rast(raster, w = 3, fun = "mean", shape = "square")
}
\arguments{
\item{raster}{A \code{SpatRaster} object from the \strong{terra} package.}

\item{w}{Integer or matrix. The size of the moving window (default = 3).
If \code{shape = "square"}, the window will be of size \verb{w x w}.
If \code{shape = "circle"}, \code{w} defines the radius of the circular window.}

\item{fun}{Function or character. The function to apply to neighboring values.
Common choices include \code{"mean"} (default), \code{"median"}, \code{"min"}, \code{"max"},
or a user-supplied function.}

\item{shape}{Character. The shape of the moving window.
Options are \code{"square"} (default) or \code{"circle"}.}
}
\value{
A \code{SpatRaster} with missing values replaced by the aggregated
neighboring values.
}
\description{
Applies a moving window operation that replaces only the missing values (\code{NA})
in a raster with an aggregate of their surrounding cells.
}
\details{
Only cells with missing values (\code{NA}) are replaced; all other cells
remain unchanged. The replacement value is computed using the
specified function applied over the defined window of neighboring cells.
}
\examples{

\dontrun{
library(terra)
r <- rast(nrows = 10, ncols = 10)
values(r) <- runif(ncell(r))
r[sample(1:ncell(r), 10)] <- NA  # insert some missing values
r_filled <- fill_missing_rast(r, w = 3, fun = "mean")
plot(c(r, r_filled))
}
}
\seealso{
\link[terra:focal]{terra::focal}
}
