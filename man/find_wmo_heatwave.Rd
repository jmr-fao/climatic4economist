% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_wmo_heatwave.R
\name{find_wmo_heatwave}
\alias{find_wmo_heatwave}
\title{Compute Heat Wave Spells (WMO Definition)}
\usage{
find_wmo_heatwave(df, excess = 5L, min_spell = 2L)
}
\arguments{
\item{df}{A data frame with an \code{ID} column and daily temperature columns
(in wide format, named with date-like strings, e.g. \code{"2000-01-01"}).}

\item{excess}{Numeric. The minimum temperature anomaly above the monthly mean
required to classify a day as "hot". Default is 5.}

\item{min_spell}{Integer. The minimum number of consecutive hot days to qualify
as a heat wave spell. Default is 2.}
}
\value{
A data frame with columns:
\itemize{
\item \code{ID}: identifier for the unit.
\item \code{date}: the date of observation.
\item \code{value}: the daily temperature.
\item \code{spell_wmo}: an integer index identifying consecutive heat wave days (0 if none).
}
}
\description{
Identifies heat wave spells according to the World Meteorological Organization (WMO) criteria:
a heat wave is defined as a sequence of consecutive days where the daily temperature
exceeds the long-term monthly average by at least a specified 5 degree Celsius.
}
\details{
For each \code{ID} and month, the monthly mean temperature is computed. A day is flagged
as part of a heat wave if its temperature exceeds the monthly mean by at least \code{excess}
and it belongs to a sequence of at least \code{min_spell} consecutive days.
}
\examples{
\dontrun{
df <- data.frame(
  ID = 1,
  `2000-01-01` = 20,
  `2000-01-02` = 22,
  `2000-01-03` = 25,
  `2000-01-04` = 28
)
find_wmo_heatwave(df, excess = 5, min_spell = 2)
}

}
