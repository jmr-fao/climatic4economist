% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_nearest_distance.R
\name{cal_nearest_distance}
\alias{cal_nearest_distance}
\title{Calculate nearest distance between two SpatVector objects}
\usage{
cal_nearest_distance(x, y, return_geometry = FALSE, ...)
}
\arguments{
\item{x}{A \code{terra::SpatVector} (polygons, points, or lines).}

\item{y}{A \code{terra::SpatVector} containing target features.}

\item{return_geometry}{Logical. If TRUE, returns a \code{SpatVector} with geometries.
If FALSE (default), returns a tibble with polygon attributes only.}

\item{...}{Additional arguments passed to \code{terra::nearest()}
(e.g., \code{method}, \code{centroids}, \code{distance}).}
}
\value{
Either a \link[terra:SpatVector-class]{terra::SpatVector} or a \link[tibble:tbl_df-class]{tibble::tbl_df} equal to \code{x} with an added column:
\itemize{
\item \code{dist_nearest_km}: minimum distance to \code{y} in kilometers.
}
}
\description{
Computes the minimum distance from each feature in \code{x} to the nearest
feature in \code{y} using \code{\link[terra:nearby]{terra::nearest()}}. Returns \code{x} with an added
column containing the nearest distance in kilometers.
}
\details{
If CRS differ, \code{y} is automatically reprojected to the CRS of \code{x}.
\itemize{
\item Distance is 0 if features intersect.
\item If CRS is geographic (lon/lat), consider using \code{method = "haversine"}.
\item For projected CRS (meters), default Euclidean distance is used.
}
}
\examples{
\dontrun{
x_with_dist <- cal_nearest_distance(
  x = polygons,
  y = roads,
  method = "haversine"
)
}

}
\seealso{
\link[terra:nearby]{terra::nearest} \link{cal_distance}
}
