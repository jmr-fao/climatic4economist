% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_distance.R
\name{calc_distance}
\alias{calc_distance}
\title{Compute Distances Between Spatial Objects (using purrr::partial)}
\usage{
calc_distance(
  x,
  y,
  fun_summ = min,
  fun_args = list(),
  use_union = TRUE,
  return_geometry = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{terra::SpatVector} (points, lines, or polygons) for which distances will be computed.}

\item{y}{A \code{terra::SpatVector} containing target features.}

\item{fun_summ}{Optional summary function (e.g., \code{min}, \code{max}, \code{mean}).
If provided, reduces distances per feature in \code{x} to a single value.
Default is \code{min}. If \code{NULL}, the full distance matrix is returned.}

\item{fun_args}{Optional list of additional arguments to pass to \code{fun_summ}.}

\item{use_union}{Logical. If TRUE (default) and \code{fun_summ} is not NULL,
\code{y} will be unioned to simplify geometry before computing distances.
Union can improve performance for large overlapping features}

\item{return_geometry}{Logical. If TRUE, returns a \code{SpatVector} with geometries.
If FALSE (default), returns a tibble with polygon attributes only.}

\item{...}{Additional arguments passed to \code{terra::distance()} (e.g., \code{method = "haversine"}).}
}
\value{
If \code{fun_summ} is provided, returns either a \link[terra:SpatVector-class]{terra::SpatVector} or
a \link[tibble:tbl_df-class]{tibble::tbl_df} with the attributes of \code{x}
plus a \code{distance} column (in km). If \code{fun_summ} is NULL, returns a numeric
distance matrix where rows correspond to features in \code{x} and columns to features in \code{y}.
}
\description{
Computes distances from each feature in \code{x} to features in \code{y}.
Optionally reduces multiple distances per feature using a summary function.
}
\seealso{
\link[terra:distance]{terra::distance} \link{calc_nearest_distance}
}
