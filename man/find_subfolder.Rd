% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_subfolder.R
\name{find_subfolder}
\alias{find_subfolder}
\title{Find a Subfolder by Name}
\usage{
find_subfolder(
  root,
  target,
  max_depth = 10,
  ignore_case = TRUE,
  first_only = TRUE
)
}
\arguments{
\item{root}{Character. Path to the root directory to start searching from.}

\item{target}{Character. Substring or regex pattern to match folder names.}

\item{max_depth}{Integer. Maximum depth of search (default = 10).}

\item{ignore_case}{Logical. If TRUE, search is case-insensitive (default = TRUE).}

\item{first_only}{Logical. If TRUE, return only the first match (default = TRUE).}
}
\value{
Character vector of matching folder paths. If no match is found, returns \code{NULL}.
}
\description{
Recursively searches for a subfolder within a root directory up to a given depth.
}
\examples{
\dontrun{
find_subfolder("data", "weather")   # find "weather" folder under "data"
find_subfolder("data", "temp", max_depth = 5, first_only = FALSE)
}

}
