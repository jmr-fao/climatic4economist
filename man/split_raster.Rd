% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_raster.R
\name{split_raster}
\alias{split_raster}
\title{This function divides a \code{SpatRaster} object into a grid of smaller tiles,
optionally adding overlap between adjacent tiles. It is useful for parallel
processing, memory management, or spatial subsetting of large rasters.}
\usage{
split_raster(rast, n_x = 10, n_y = 10, overlap = 0)
}
\arguments{
\item{rast}{A \code{terra::SpatRaster} object to be divided.}

\item{n_x}{Integer. Number of splits (tiles) along the x-axis (longitude).}

\item{n_y}{Integer. Number of splits (tiles) along the y-axis (latitude).}

\item{overlap}{Numeric. Optional overlap between adjacent tiles, in map units
of the raster’s coordinate reference system (e.g., meters or degrees).}
}
\value{
A named list of \code{SpatRaster} objects, each representing a tile of the
original raster. Tile names follow the pattern \code{"tile_001"}, \code{"tile_002"}, etc.
}
\description{
This function divides a \code{SpatRaster} object into a grid of smaller tiles,
optionally adding overlap between adjacent tiles. It is useful for parallel
processing, memory management, or spatial subsetting of large rasters.
}
\details{
The raster is divided evenly along the x and y axes according to \code{n_x} and
\code{n_y}. Each resulting tile is cropped from the original raster based on the
computed sub-extent. If \code{overlap > 0}, each tile is extended by the specified
distance in all directions before cropping, which can be useful to avoid edge
effects in analyses requiring neighborhood operations.
}
\examples{
\dontrun{
library(terra)
r <- rast(ncols = 100, nrows = 100)
values(r) <- runif(ncell(r))

# Split into a 4x4 grid without overlap
tiles <- split_raster(r, n_x = 4, n_y = 4)

# Split into a 2x2 grid with 10-meter overlap
tiles_overlap <- split_raster(r, n_x = 2, n_y = 2, overlap = 10)

# Plot one of the tiles
plot(tiles[[1]])
}

}
