% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate_frequency.R
\name{aggregate_frequency}
\alias{aggregate_frequency}
\title{Aggregate Time Series Data to a Target Frequency}
\usage{
aggregate_frequency(
  x,
  iteracation = NULL,
  target_freq = "\%Y-\%m-\%d",
  agg_fn = "mean"
)
}
\arguments{
\item{x}{A \code{SpatRaster} object with a time dimension.}

\item{iteracation}{optional character to be print before computation. Usually,
it is the name of the object on which the function is applied. This is useful
when the function is used inside an apply family function to keep track of the
iterations.}

\item{target_freq}{A character string specifying the target time frequency.
The default for the format methods is \code{"\%Y-\%m-\%d"}.}

\item{agg_fn}{A function to apply for aggregation (e.g., \code{mean}, \code{sum},
\code{max}, \code{min}).}
}
\value{
A \code{SpatRaster} object aggregated to the specified time frequency.
}
\description{
This function aggregates a time series raster (\code{SpatRaster} object) to a
specified target frequency using a user-defined aggregation function.
}
\examples{
# Create a sample SpatRaster with daily data
time_seq <- seq(as.Date("2020-01-01"), as.Date("2020-12-31"), by = "day")
r <- terra::rast(nrows=10, ncols=10, nlyrs=length(time_seq))
terra::time(r) <- time_seq

# Aggregate to monthly means
r_monthly <- aggregate_frequency(r, target_freq = "\%Y-\%m", agg_fn = mean)
}
