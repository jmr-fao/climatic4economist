% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extr_day_by_interview.R
\name{extr_spell_by_interview}
\alias{extr_spell_by_interview}
\title{Summarise spell indicators over the periods before each interview}
\usage{
extr_spell_by_interview(
  survey,
  indicators,
  by,
  id,
  interview,
  interval,
  n_lags = 0,
  extra_col = NULL,
  iteration = NULL
)
}
\arguments{
\item{survey}{A data frame with one row per unit (typically a household),
containing the \code{by}, \code{id} and \code{interview} columns.}

\item{indicators}{A data frame of spell data with a \code{date} column, the \code{by}
column and columns starting with \code{spell}, as returned by
\code{\link[=find_spell]{find_spell()}} or \code{\link[=find_extr_spell_rel]{find_extr_spell_rel()}}.}

\item{by}{<\code{\link[rlang:args_data_masking]{data-masking}}> The column linking
\code{survey} to \code{indicators}, typically the location \code{ID} created by
\code{\link[=prepare_coord]{prepare_coord()}}.}

\item{id}{<\code{\link[rlang:args_data_masking]{data-masking}}> The column identifying
each unit of the result, typically the household identifier.}

\item{interview}{<\code{\link[rlang:args_data_masking]{data-masking}}> The column
holding the interview dates, or a single date applied to every row.}

\item{interval}{A string specifying the reference period, e.g. \code{"1 year"},
\code{"3 months"}, \code{"30 days"}.}

\item{n_lags}{Integer. The highest lag, in units of \code{interval}, to compute.
\code{0} covers only the period immediately before the interview. Default \code{0}.}

\item{extra_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Optional columns
of \code{survey} carried through unchanged.}

\item{iteration}{Optional character printed before computation. Useful when
the function is called inside an apply family function to keep track of the
iterations.}
}
\value{
A tibble with one row per \code{id} and \code{lag}, containing for each spell
column its longest (\verb{_max}), average (\verb{_mean}) and total (\verb{_sum}) length,
and the number of spells (\verb{_n}).
}
\description{
Aggregates spell-length data into summary statistics per unit and lag,
counting only the spells ending in the reference periods preceding each
unit's interview date.
}
\details{
This is the fast counterpart to \code{\link[=extr_spell_index]{extr_spell_index()}}, and works exactly as
\code{\link[=extr_day_by_interview]{extr_day_by_interview()}} does — see its Details for how the reference
windows are built and why the intermediate merge is avoided.
}
\examples{
survey <- data.frame(
  hhid = 1:2,
  ID = c("1", "1"),
  interview = as.Date("2024-01-01")
)
spells <- data.frame(
  ID = "1",
  date = as.Date(c("2023-06-01", "2023-07-01")),
  spell_abv_90p = c(3, 5)
)
extr_spell_by_interview(survey, spells, by = ID, id = hhid,
                        interview = interview, interval = "1 year")
}
\seealso{
\code{\link[=extr_day_by_interview]{extr_day_by_interview()}} for daily data, \code{\link[=extr_spell_index]{extr_spell_index()}} for
the merge-first equivalent.
}
