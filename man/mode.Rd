% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mode.R
\name{mode}
\alias{mode}
\title{Compute the mode of a vector}
\usage{
mode(x, ties = c("first", "all", "NA", "error"))
}
\arguments{
\item{x}{A vector.}

\item{ties}{Character string specifying how ties should be handled.
One of:
\describe{
\item{"first"}{Return the first mode encountered (default).}
\item{"all"}{Return all modes.}
\item{"NA"}{Return a missing value if multiple modes exist.}
\item{"error"}{Throw an error if the mode is not unique.}
}}
}
\value{
Depending on \code{ties}:
\itemize{
\item \code{"first"} returns a single value of the same type as \code{x}.
\item \code{"all"} returns a vector containing all modal values.
\item \code{"NA"} returns a missing value of the same type as \code{x} when ties
occur.
\item \code{"error"} returns a single value if the mode is unique, otherwise
throws an error.
}

If \code{x} contains only missing values, a missing value of the same type as
\code{x} is returned.
}
\description{
Returns the most frequent value in a vector, optionally handling ties in
different ways. Missing values are ignored.
}
\examples{
mode(c(1, 2, 2, 3))

mode(c(1, 1, 2, 2), ties = "all")

mode(c("a", "b", "a"))

mode(c(NA, 1, 1, 2))

}
